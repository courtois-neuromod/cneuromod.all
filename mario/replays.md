# replays

This codebase can be used to generate replays from the behavioral files of the mario dataset. For each repetition, it will create files containing metadata (.json), video (.gif, .mp4 or .webp) and memory or game variables dumps (.npz)
